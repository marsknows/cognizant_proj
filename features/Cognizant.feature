Feature: Cognizant Assignment

Scenario: Saucedemo practice
    Given The user navigates to 'https://www.saucedemo.com/'
    When the user enters the username and password
    When the user clicks the login element
    When the user clicks the add backpack element
    When the user clicks the add bike light element
    When the user clicks the add t shirt element
    When the the user validates the backpack element is visible
    When the the user validates the bike light element is visible
    When the the user validates the t shirt element is visible
    When the the user validates the jacket element is visible
    When the the user validates the onesie element is visible
    When the the user validates the red shirt element is visible
    When the user clicks the cart element
    When the user clicks the remove bike element
    When the user clicks the checkout element
    When the user fills the page with user info
    When the user clicks the continue element
    When the user clicks the t shirt element
    When the user clicks the remove element
    When the user clicks the cart element
    When the user clicks the checkout element
    When the user fills the page with user info
    When the user clicks the continue element
    When the user clicks the finish element
    Then the the user validates the confirmation element is visible
    When the user clicks the back home element
    When the user clicks the menu element
    When the user clicks the logout element
